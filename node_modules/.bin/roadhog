#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_roadhog@1.4.1@roadhog/bin/roadhog.js" "$@"
  ret=$?
else 
  node  "$basedir/../_roadhog@1.4.1@roadhog/bin/roadhog.js" "$@"
  ret=$?
fi
exit $ret
