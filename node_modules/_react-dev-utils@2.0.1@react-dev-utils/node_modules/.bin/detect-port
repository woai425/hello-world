#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_@timer_detect-port@1.1.3@@timer/detect-port/bin/detect-port" "$@"
  ret=$?
else 
  node  "$basedir/../../../_@timer_detect-port@1.1.3@@timer/detect-port/bin/detect-port" "$@"
  ret=$?
fi
exit $ret
